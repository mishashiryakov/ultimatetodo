export const styles = theme => ({
  layout: {
    height: '100vh',
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: theme.palette.background.main
  },

  main: {
    display: 'flex',
    flexDirection: 'column',
    flexGrow: 1,
  }
});